export default function Loading() {
    return(
        <main className="text-center">
            <h2 className="text-teal-900">Loading....</h2>
            <p>Wait a few seconds..</p>
        </main>
    )
}